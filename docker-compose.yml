# Local development only — do NOT use these credentials in production.
services:
  postgres:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: uniclaw
      POSTGRES_USER: uniclaw
      POSTGRES_PASSWORD: uniclaw  # dev-only, change for any non-local deployment
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U uniclaw"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
